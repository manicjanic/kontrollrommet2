class ParticipantObj {
    constructor() {
        person_uuid = "uuid"
        requester = "date-time"
        invitee = "date-time"
        responder = "date-time"
        attender = "date-time"
        logger = "date-time"
        attended = "date-time"
        reporter = "date-time"
        reported = "date-time"
    }
}
